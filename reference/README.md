# reference/

Optional. If you have the finished code of the tutorial (the creator's
GitHub repo, a zip, a code attachment), put it here.

- Paste it as-is, keeping the tutorial's folder structure inside
  `reference/` (e.g. `reference/src/App.tsx`). Or paste each file as a
  `.txt` if you prefer; both work.
- This is YOUR code, pasted by you. Claude only reads it.
- Claude treats it as the source of truth and replicates it exactly, in
  the same order the transcript builds it. Claude never "improves" it and
  never invents code the reference does not contain.
- If the transcript and the reference disagree, Claude will point at the
  exact spot and ask you which to follow.

Commit, push, and mention it when you say "kickoff".
