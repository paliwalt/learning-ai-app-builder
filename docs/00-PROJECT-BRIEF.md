# Project Brief

_Filled in by Claude during `/kickoff`, one question per turn. Do not start
the roadmap until every empty field below has an answer._

## What we are building
- **Project name:**
- **One-sentence description:**
- **Who it is for / what problem it solves:**
- **Default:** exactly what the transcripts build, unless the Learner changes it below.

## Source of truth
- **Transcripts:** `transcripts/` (list the files)
- **Reference code:** `reference/` (finished tutorial code, if provided) — replicate it exactly
- **Gaps the source does not cover:** (Claude lists them here as they are found, and asks before filling any)

## Scope
- **Core features (from the transcripts):**
- **Extra features the Learner wants (beyond the transcripts):**
- **Explicitly out of scope:**

## Stack
- **Languages / frameworks / tools (from transcripts):**
- **Anything the Learner wants to swap or add:**

## Pinned versions (never a bare install)
| Package / tool | Version | Source (transcript / reference / Learner chose) |
|----------------|---------|--------------------------------------------------|
| node | | |
| | | |

## The Learner
- **Goal:** become an expert-level engineer (the 1% of the 1%), not just
  finish the tutorial. Every step carries a pro note; every module ends with
  a concept check and own-words notes in `docs/NOTES.md`.
- **Level per technology:** (e.g. TypeScript: never / React: seen it / git: can commit)
- **Fresh start or resuming? If resuming, where did they get lost:**
- **Already comfortable with:**
- **Wants to go slower on / faster on:**
- **Operating system + editor + terminal:**

## Repo layout decision
- **Where project code lives (`/` or `src/` or `app/`):**
- **PR granularity (one PR per step / per few steps / per module):**
- **End-of-section ritual:** branch → commit → push → PR → merge → `git pull origin main` → Claude runs `/pr-sync`
